import { CountUpNumbersDirective } from './count-up-numbers.directive';

describe('CountUpNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new CountUpNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
